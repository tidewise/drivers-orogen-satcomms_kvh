# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(satcomms_kvhTaskLib)
ADD_LIBRARY(${SATCOMMS_KVH_TASKLIB_NAME} SHARED
    ${SATCOMMS_KVH_TASKLIB_SOURCES})
add_dependencies(${SATCOMMS_KVH_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${SATCOMMS_KVH_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${SATCOMMS_KVH_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SATCOMMS_KVH_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SATCOMMS_KVH_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${SATCOMMS_KVH_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${SATCOMMS_KVH_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${SATCOMMS_KVH_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SATCOMMS_KVH_TASKLIB_HEADERS}
    DESTINATION include/orocos/satcomms_kvh)

